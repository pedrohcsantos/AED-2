multiplica()
string op1_str, op2_str;
int op1[4], op2[4], r[8], aux_multi, aux_div, R_aux[5];
escreva('Informe o 1° operador: ');
leia(op1_str);
escreva('Informe o 2° operador: ');
leia(op2_str);
para i = 1 até 4 faça:
    op1[i] = int(op1_str[i]);
    op2[i] = int(op2_str[i]);
fim para;
para i = 1 até até 8 faça
    R[i] = 0;
Para i = 4 até(downto) 1  // percorre op2
    aux_div = 0;
    Para j = 4 até(downto) 1 faça // percorre op1
        aux_multi = op1[i] * op2[i] + aux_div;
        R_aux[j+1] = aux_multi mod 10;
        aux_div = aux_multi div 10;
    Fim;
    R_aux[1] = aux_div, aux_div = 0;
    Para j = 4 até(downto) 0 faça
        aux_soma = R_aux[j+1]+R[i+j] + aux_div;
        R[i+j] = aux_soma div 10;
        aux_div = aux_soma mod 10;
    Fim para
Fim para
Mostre(R);
Fim algoritmo.    